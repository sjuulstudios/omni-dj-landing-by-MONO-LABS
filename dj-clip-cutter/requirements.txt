flask>=3.0
librosa>=0.10
numpy
scipy
soundfile
ffmpeg-python
requests
supabase>=2.0
python-dotenv>=1.0
stripe>=8.0

# SESSIE 21 — Brand Stack font conversion (WOFF / WOFF2 → TTF / OTF).
# Optional at runtime: if absent, WOFF/WOFF2 uploads are rejected with a
# clear error and TTF/OTF still work. Install with:
#   pip install "fonttools[woff]"
fonttools[woff]>=4.40

# SESSIE 22 D — Auto-tracking dependencies. All optional at runtime;
# the /api/track/<job>/<clip>/auto endpoint returns 422 with an install
# hint if none are available. Telemetry is hard-killed in tracking.py
# before ultralytics is imported (YOLO_OFFLINE=True).
#   pyobjc-framework-Vision — macOS-native person detection (privacy-first
#     primary engine). macOS-only; pip-installable on darwin platforms.
#   opencv-python           — frame I/O + HOG last-resort detector.
#   ultralytics             — YOLOv8 fallback for lowlight / occluded frames.
# Run on macOS:
#   pip install "pyobjc-framework-Vision" opencv-python ultralytics
opencv-python>=4.9
ultralytics>=8.2
pyobjc-framework-Vision>=10.0; sys_platform == "darwin"

# Optional: AI-powered source separation (better drop detection)
# Install with: pip install torch demucs
# torch
# demucs

# Optional: YouTube upload support
# google-api-python-client
# google-auth-oauthlib
